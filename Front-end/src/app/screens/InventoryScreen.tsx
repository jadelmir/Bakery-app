import { useMemo, useState } from "react";
import type { InventoryItem, Task } from "../types";
import { SectionHeader } from "../components/shared/SectionHeader";
import { InventoryAdjustModal, type InventoryCommand, type InventoryEntryAction } from "../components/inventory/InventoryAdjustModal";
import { ShoppingListDrawer, type ShoppingListItem } from "../components/inventory/ShoppingListDrawer";
import { InventoryItemCreateDialog, type InventoryItemDraft } from "../components/inventory/InventoryItemCreateDialog";
import { buildStarterPlans, calculateRequirements, type InventoryTransaction } from "../planning";
import type { ProductionTask } from "../production";
import { AlertTriangle, ClipboardList, PackagePlus, ShoppingBag } from "lucide-react";

export interface InventoryOverviewItem extends InventoryItem {
  readonly kind?: "ingredient" | "packaging" | "finished_good";
  readonly onHand?: number;
  readonly reserved?: number;
  readonly required?: number;
  readonly allocated?: number;
  readonly packageQuantity?: number;
  readonly packagePrice?: number;
  readonly unitCost?: number;
}
export interface InventoryScreenProps {
  tasks: Task[];
  builds: ReturnType<typeof buildStarterPlans>;
  transactions: InventoryTransaction[];
  ingredients?: readonly InventoryItem[];
  /** Enriched display input that an inventory adapter may supply without changing shared domain contracts. */
  inventoryItems?: readonly InventoryOverviewItem[];
  onReceivePurchase?: (command: Extract<InventoryCommand, { type: "receive" }>) => Promise<void> | void;
  onPhysicalCount?: (command: Extract<InventoryCommand, { type: "physical-count" }>) => Promise<void> | void;
  onRelativeAdjustment?: (command: Extract<InventoryCommand, { type: "relative-adjustment" }>) => Promise<void> | void;
  onUpdateInventoryItem?: (command: Extract<InventoryCommand, { type: "edit-item" }>) => Promise<void> | void;
  onDeleteInventoryItem?: (command: Extract<InventoryCommand, { type: "delete-item" }>) => Promise<void> | void;
  onCreateInventoryItem?: (draft: InventoryItemDraft) => Promise<void> | void;
}

const groupLabels = { ingredient: "Ingredients", packaging: "Retail supplies", finished_good: "Finished goods (production output)" } as const;
const number = (value: number) => value.toLocaleString(undefined, { maximumFractionDigits: 2 });

export function InventoryScreen({ tasks, builds, transactions, ingredients, inventoryItems, onReceivePurchase, onPhysicalCount, onRelativeAdjustment, onUpdateInventoryItem, onDeleteInventoryItem, onCreateInventoryItem }: InventoryScreenProps) {
  const [selectedOrder, setSelectedOrder] = useState("all"); const [entryOpen, setEntryOpen] = useState(false); const [selectedItemId, setSelectedItemId] = useState<string>(); const [entryAction, setEntryAction] = useState<InventoryEntryAction>("receive"); const [shoppingOpen, setShoppingOpen] = useState(false); const [createItemOpen, setCreateItemOpen] = useState(false);
  const items = (inventoryItems ?? ingredients ?? []) as readonly InventoryOverviewItem[];
  const requirements = useMemo(() => calculateRequirements(tasks as unknown as ProductionTask[], builds, items.filter(item => (item.kind ?? "ingredient") !== "finished_good").map(item => ({ id: item.id, name: item.name, unit: item.unit, onHand: item.onHand ?? item.current, kind: item.kind === "packaging" ? "packaging" as const : "ingredient" as const })), undefined, selectedOrder === "all" ? undefined : selectedOrder), [tasks, builds, items, selectedOrder]);
  const plannedById = useMemo(() => requirements.reduce<Record<string, number>>((totals, line) => ({ ...totals, [line.itemId]: (totals[line.itemId] ?? 0) + line.required }), {}), [requirements]);
  const overview = items.map(item => {
    const kind = item.kind ?? "ingredient"; const onHand = item.onHand ?? item.current; const reserved = item.reserved ?? 0; const allocated = item.allocated ?? 0; const required = item.required ?? plannedById[item.id] ?? 0; const available = onHand - reserved - (kind === "finished_good" ? allocated : 0); const demandShortage = Math.max(0, required - available); const minimumShortage = Math.max(0, item.minLevel - available); const shortage = Math.max(demandShortage, minimumShortage); const shortageReason: ShoppingListItem["shortageReason"] = demandShortage > 0 && minimumShortage > 0 ? "both" : demandShortage > 0 ? "demand" : "minimum";
    return { ...item, kind, onHand, reserved, allocated, required, available, shortage, shortageReason };
  });
  const shortages = overview.filter(item => item.shortage > 0); const grouped = (["ingredient", "packaging"] as const).map(kind => [kind, overview.filter(item => item.kind === kind)] as const); const finishedGoods = overview.filter(item => item.kind === "finished_good");
  const dispatch = async (command: InventoryCommand) => { if (command.type === "edit-item" && onUpdateInventoryItem) return onUpdateInventoryItem(command); if (command.type === "delete-item" && onDeleteInventoryItem) return onDeleteInventoryItem(command); if (command.type === "receive" && onReceivePurchase) return onReceivePurchase(command); if (command.type === "physical-count" && onPhysicalCount) return onPhysicalCount(command); if (command.type === "relative-adjustment" && onRelativeAdjustment) return onRelativeAdjustment(command); throw new Error("This inventory command is waiting for workspace integration."); };
  const openItemEditor = (itemId: string) => { setSelectedItemId(itemId); setEntryAction("edit-item"); setEntryOpen(true); };
  const openStockEntry = (itemId?: string) => { setSelectedItemId(itemId); setEntryAction("receive"); setEntryOpen(true); };
  const shoppingItems: ShoppingListItem[] = overview.map(item => ({ itemId: item.id, name: item.name, unit: item.unit, onHand: item.onHand, reserved: item.reserved, available: item.available, minLevel: item.minLevel, required: item.required, shortage: item.shortage, shortageReason: item.shortageReason, packageQuantity: item.packageQuantity, packagePrice: item.packagePrice, unitCost: item.unitCost }));
  return <div className="mx-auto max-w-6xl px-4 py-6 pb-28 lg:pb-10"><header className="mb-5 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h1 className="text-xl font-extrabold text-[#2F2925]">Inventory</h1><p className="mt-1 text-xs text-[#988D84]">Ingredients and retail supplies, with production output tracked separately.</p></div><div className="flex flex-wrap gap-2"><button onClick={() => setShoppingOpen(true)} className="h-9 rounded-[10px] border border-[#2D7A46]/20 bg-[#EBF4EC] px-3 text-sm font-bold text-[#2D7A46]"><ShoppingBag className="mr-1 inline" size={14} />Shopping list {shortages.length ? `(${shortages.length})` : ""}</button><button onClick={() => setCreateItemOpen(true)} className="h-9 rounded-[10px] border border-[#7A3E24]/20 bg-white px-3 text-sm font-bold text-[#7A3E24]"><PackagePlus className="mr-1 inline" size={14} />Add item</button><button onClick={() => openStockEntry()} className="h-9 rounded-[10px] bg-[#7A3E24] px-3 text-sm font-bold text-white"><PackagePlus className="mr-1 inline" size={14} />Record inventory</button></div></header>
    <label className="mb-4 block text-xs text-[#6F655E]">Requirements for <select aria-label="Inventory order filter" value={selectedOrder} onChange={event => setSelectedOrder(event.target.value)} className="ml-2 h-8 rounded border border-[#E5DDD3] bg-white px-2"><option value="all">all scheduled orders</option>{[...new Set(tasks.map(task => task.orderId).filter(Boolean))].map(orderId => <option key={orderId} value={orderId}>{orderId}</option>)}</select></label>
    {shortages.length > 0 && <div className="mb-5 flex gap-2 rounded-[14px] border border-[#B8443C]/20 bg-[#FCE9E7] p-3 text-sm font-semibold text-[#B8443C]"><AlertTriangle size={16} />{shortages.length} item{shortages.length === 1 ? "" : "s"} need attention. Availability already accounts for reserved stock.</div>}
    <div className="space-y-5">{grouped.map(([kind, group]) => <section key={kind}><SectionHeader title={groupLabels[kind]} />{group.length === 0 ? <p className="rounded-[14px] border border-dashed border-[#E5DDD3] p-4 text-sm text-[#6F655E]">No {groupLabels[kind].toLowerCase()} tracked yet.</p> : <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{group.map(item => <article key={item.id} onClick={event => { if (!(event.target as HTMLElement).closest("button")) openItemEditor(item.id); }} className="cursor-pointer rounded-[14px] border border-[#E5DDD3] bg-white p-4"><div className="flex justify-between gap-3"><button type="button" onClick={() => openItemEditor(item.id)} className="text-left" title={`Edit ${item.name} or add stock`}><h3 className="font-bold text-[#2F2925]">{item.name}</h3><p className="text-xs text-[#6F655E]">Required {number(item.required)}{item.unit}</p></button><button type="button" onClick={() => openStockEntry(item.id)} className="text-xs font-bold text-[#7A3E24]">Record</button></div><dl className="mt-3 grid grid-cols-2 gap-x-3 gap-y-2 text-xs"><div><dt className="text-[#988D84]">On hand</dt><dd className="font-bold">{number(item.onHand)}{item.unit}</dd></div><div><dt className="text-[#988D84]">Reserved</dt><dd className="font-bold">{number(item.reserved)}{item.unit}</dd></div><div><dt className="text-[#988D84]">Available</dt><dd className={`font-bold ${item.available < 0 ? "text-[#B8443C]" : ""}`}>{number(item.available)}{item.unit}</dd></div><div><dt className="text-[#988D84]">Shortage</dt><dd className={`font-bold ${item.shortage ? "text-[#B8443C]" : "text-[#2D7A46]"}`}>{item.shortage ? `${number(item.shortage)}${item.unit}` : "None"}</dd></div></dl>{item.available < 0 && <p className="mt-3 flex gap-1 text-xs font-semibold text-[#B8443C]"><AlertTriangle size={13} />Negative stock: production can continue, but replenish this item.</p>}{item.shortage > 0 && <p className="mt-2 text-xs text-[#B8443C]">{item.shortageReason === "minimum" ? "Shortage is from the minimum-level rule." : item.shortageReason === "both" ? "Shortage is from scheduled demand and the minimum-level rule." : "Shortage is from scheduled demand after reservations."}</p>}</article>)}</div>}</section>)}{finishedGoods.length > 0 && <section><SectionHeader title={groupLabels.finished_good} /><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{finishedGoods.map(item => <article key={item.id} onClick={event => { if (!(event.target as HTMLElement).closest("button")) openItemEditor(item.id); }} className="cursor-pointer rounded-[14px] border border-[#E5DDD3] bg-white p-4"><div className="flex justify-between gap-3"><button type="button" onClick={() => openItemEditor(item.id)} className="text-left" title={`Edit ${item.name} or add stock`}><h3 className="font-bold text-[#2F2925]">{item.name}</h3><p className="text-xs text-[#6F655E]">Production output</p></button><button type="button" onClick={() => openStockEntry(item.id)} className="text-xs font-bold text-[#7A3E24]">Record</button></div><dl className="mt-3 grid grid-cols-3 gap-2 text-xs"><div><dt className="text-[#988D84]">On hand</dt><dd className="font-bold">{number(item.onHand)}{item.unit}</dd></div><div><dt className="text-[#988D84]">Reserved</dt><dd className="font-bold">{number(item.reserved)}{item.unit}</dd></div><div><dt className="text-[#988D84]">Available</dt><dd className="font-bold">{number(item.available)}{item.unit}</dd></div></dl></article>)}</div></section>}</div>
    <section className="mt-5"><SectionHeader title="Stock history" /><div className="rounded-[14px] border border-[#E5DDD3] bg-white">{transactions.length === 0 ? <p className="p-4 text-sm text-[#6F655E]">No inventory events recorded yet.</p> : transactions.slice().reverse().slice(0, 10).map(transaction => { const item = overview.find(candidate => candidate.id === transaction.itemId); return <div key={transaction.id} className="flex items-center justify-between border-b border-[#F0E9E0] p-3 text-sm last:border-0"><span><ClipboardList className="mr-2 inline text-[#7A3E24]" size={14} />{item?.name ?? transaction.itemId} · {transaction.reason}</span><strong className={transaction.quantityChange < 0 ? "text-[#B8443C]" : "text-[#2D7A46]"}>{transaction.quantityChange > 0 ? "+" : ""}{number(transaction.quantityChange)}{item?.unit ?? ""}</strong></div>; })}</div></section>
    <InventoryAdjustModal isOpen={entryOpen} onClose={() => setEntryOpen(false)} selectedItemId={selectedItemId} initialAction={entryAction} items={overview.map(item => ({ id: item.id, name: item.name, unit: item.unit, kind: item.kind, minLevel: item.minLevel, onHand: item.onHand, packageQuantity: item.packageQuantity, packagePrice: item.packagePrice }))} onCommand={dispatch} />
    <InventoryItemCreateDialog isOpen={createItemOpen} onClose={() => setCreateItemOpen(false)} onSubmit={async draft => { if (!onCreateInventoryItem) throw new Error("Inventory item creation is not available for this workspace."); await onCreateInventoryItem(draft); }} />
    <ShoppingListDrawer isOpen={shoppingOpen} onClose={() => setShoppingOpen(false)} items={shoppingItems} />
  </div>;
}
