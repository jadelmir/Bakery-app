## 1. Supabase recipe persistence boundary

- [x] 1.1 Create an ordered migration with the repository-pinned Supabase CLI that adds the normalized `recipe_ingredients` relation, quantity/foreign-key/index constraints, converts nullable `recipes.flow_id` to the string-ID type used by persisted production flows, adds authenticated bakery-membership RLS policies, and grants the required access. Do not repurpose `ingredients.recipe_id` or edit historical migrations. Evidence: `20260816011229_recipe_persistence.sql` applies during a local no-seed reset.
- [x] 1.2 Add a membership-checked atomic recipe-save boundary for create/update that upserts the recipe row, replaces ingredient quantities, validates that recipe and inventory rows belong to the requested bakery, preserves nullable `flow_id`, and returns authoritative recipe data. Cover same-bakery success, cross-bakery denial, anonymous denial, malformed/duplicate IDs, and failed atomic-write behavior with rollback-safe database tests. Evidence: focused `recipe_persistence.test.sql` passes 21/21 checks locally.
- [x] 1.3 Regenerate/check `Front-end/src/lib/supabase/database.types.ts` from the local schema and verify migration order, grants, RLS policies, and RPC privileges using the repository's local Supabase reset/lint/advisor workflow. Evidence: local type generation succeeded, migration `20260816011229` is applied, database lint has no schema errors, and security advisors report no issues.

## 2. Authenticated frontend adapter and workspace integration

- [x] 2.1 Implement a bakery-scoped Supabase recipe adapter under `Front-end/src/features/recipes/` that loads recipe rows plus ingredient lines, maps cents to domain dollars, maps nullable flow assignments, derives current ingredient costs from loaded inventory, and maps Supabase failures to the existing adapter error contract. Evidence: `recipeAdapter.ts` loads bakery-filtered rows/lines, maps cents and nullable flows, and maps RPC/query failures through the adapter contract.
- [x] 2.2 Wire the authenticated `BakeryWorkspace` adapter composition to load persisted recipes into `recipesById` and delegate recipe create/update mutations to the Supabase adapter. Generate valid UUIDs for persisted recipe creation while preserving local/mock fixture behavior and existing manual-order recipe projections. Evidence: `BakeryWorkspace.tsx` loads recipes during persisted snapshot hydration, delegates create/update, and retains the manual-order fallback projection.
- [x] 2.3 Make Recipe Manager create/update callbacks awaitable and authoritative: keep the draft open and surface a persistence error on failure; after success, render the returned recipe and ingredient lines rather than an unverified optimistic row. Preserve the optional production-flow assignment behavior owned by `optional-recipe-production-flow`. Evidence: RecipeManager and RecipeEditorDialog await save callbacks, close only after success, surface failures, and preserve flow-builder assignment behavior.
- [x] 2.4 Add focused adapter, workspace/domain-load, and component tests proving recipe create/update round-trip, ingredient-line round-trip, reload survival, current-cost recalculation, nullable flow assignment, invalid-ID handling, persistence failure handling, and local/mock regression behavior. Evidence: recipe adapter and component tests pass 9/9, and the existing workspace/local regression suite remains covered by `workspace.test.tsx`.

## 3. Verification and traceability

- [x] 3.1 Run the affected Supabase database tests and local reset, RLS/security assertions, generated-type check, focused recipe/flow tests, and the frontend baseline (`pnpm run typecheck`, `pnpm run lint`, `pnpm run test`, `pnpm run build`). Run the affected Playwright recipe journey when its authenticated local precondition is available; record exact unrelated blockers instead of weakening scope. Evidence: focused DB tests pass 21/21; no-seed local reset, migration list, DB lint, and security advisors pass; typecheck, lint, and build pass; focused recipe tests pass 9/9. A seeded reset remains blocked by the pre-existing `ingredients_base_unit_check` seed violation. Full frontend suite is 289/296 with 7 pre-existing failures in `App.test.tsx`, `OrdersScreen.test.tsx`, `ProductionTimeBlock.test.tsx`, and `ProductionScreen.timeline.test.tsx`; the authenticated recipe Playwright precondition is unavailable.
- [x] 3.2 Update `openspec/PROGRAM_MAP.md` to record `persist-recipes` as the sole active owner of the recipe persistence delta for F4/B4, and update `docs/database/database-schema.md` plus any affected current-system API/architecture reference with the new relation/RPC and verification evidence. Do not claim hosted rollout or archive readiness from local evidence alone. Evidence: PROGRAM_MAP, database schema reference, and API reference now document the recipe relation, atomic save RPC, local verification, and hosted-acceptance boundary.
